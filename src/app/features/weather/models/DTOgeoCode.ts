export interface LocationInfo {
  city: string;
  locality: string;
  countryName: string;
  countryCode: string;
  principalSubdivision: string; // Ejemplo: "Lima" (Departamento)
  latitude: number;
  longitude: number;
  continent: string;
}
